= TODO

= Installation

First, install the following packages

```
apt-get install make automake g++ libboost-dev libboost-regex-dev libgmp-dev
```

Then execute `make` in the problemtools subdirectory to install the problemtools.
